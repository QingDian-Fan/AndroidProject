package com.dian.demo.repository.local

interface DatabaseDao {
    /**
     * 样例：
     * @Insert(onConflict = OnConflictStrategy.REPLACE)
     * void insertDatabaseEntities(List<DatabaseEntity> coinEntity);
     */

}