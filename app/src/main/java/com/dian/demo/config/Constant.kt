package com.dian.demo.config

import android.os.Environment

object Constant {
    const val DEBUG_URL_CONFIG = "debug_url_config"
    var apkPath =  Environment.getExternalStorageDirectory().path + "/AndroidProject/apk/"
}